import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  title: {
    fontSize: 16,
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default globalStyles;
